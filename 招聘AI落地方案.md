# 《AI 对话式招聘数据管家》落地方案

> 基于大模型(LLM)的招聘数据自动化记录 · 实时同步 · 可视化看板
> 目标:让 HR 从"填表"变成"说话",最小成本实现招聘流程自动化、智能化。

---

## 一、背景与痛点

### 1.1 现状

现有招聘流程中,HR 需要手动记录、跟踪每一位候选人的招聘数据,日常工作载体为 **企业微信、企业微信群、腾讯在线文档**。

### 1.2 痛点

| 痛点 | 具体表现 | 影响 |
| --- | --- | --- |
| 人工录入效率低 | 每天面试多人,HR 需逐个在在线文档中新建行、填字段 | 大量时间被"搬运"工作占用 |
| 数据易遗漏 | 消息分散在企业微信群里,容易被刷屏淹没、漏记漏更新 | 候选人跟进断档,Offer 流失 |
| 时效性差 | 线上文档需人工维护,领导要数据时往往滞后或不准 | 决策缺乏实时依据 |
| 统计靠手工 | 漏斗、通过率、各阶段人数需人工数数/写公式 | 出错率高、重复劳动 |

### 1.3 目标

- **自动化记录**:HR 在企业微信/群里用一句话汇报招聘进展,系统自动录入结构化数据
- **实时同步更新**:数据即时入库、自动同步腾讯在线文档,无需人工维护
- **数据可视化**:招聘漏斗、阶段分布、趋势图实时呈现,供 HR/管理层随时查看
- **解放人工**:把 HR 从录入、统计中解放出来,聚焦面试判断与候选人沟通

---

## 二、总体方案设计

### 2.1 核心思路

> **一句话方案:把"HR 手动填表"升级为"AI 听懂人话并自动记录"。**

HR 在熟悉的企业微信/群里,用自然语言汇报(如「张三 后端工程师 通过二面 期望 15k」),AI 大模型自动解析为结构化数据写入系统,并实时同步到腾讯在线文档和可视化看板。

### 2.2 系统架构

```
┌────────────────────────────────────────────────────────────────┐
│                         数据接入层                              │
│    企业微信 智能机器人(长连接)/ HR 手动补录 / 批量导入          │
└──────────────────────────────┬─────────────────────────────────┘
                               ▼
┌────────────────────────────────────────────────────────────────┐
│                          AI 语义解析层                          │
│   大模型(DeepSeek) 自然语言 → 结构化 JSON                      │
│   ↓ 校验 / 清洗 / 去重 / 同音字纠错(规则引擎兜底)              │
└──────────────────────────────┬─────────────────────────────────┘
                               ▼
┌────────────────────────────────────────────────────────────────┐
│                          数据存储层                             │
│            SQLite 轻量数据库(候选人/阶段/状态/薪资)            │
└───────────────┬───────────────────────────────┬────────────────┘
                ▼                               ▼
┌──────────────────────────────┐   ┌──────────────────────────────┐
│         输出层 · 实时同步      │   │       输出层 · 可视化        │
│ 腾讯在线文档(CSV 自动同步/API) │   │   Web 招聘看板(ECharts)     │
│ 导出 Excel/CSV 周报            │   │  漏斗/阶段/趋势/候选人明细  │
└──────────────────────────────┘   └──────────────────────────────┘
```

### 2.3 业务流程

1. HR 在企业微信群中说:「今天张三过了一面,后端岗,期望 15k,二面约周四」
2. 智能机器人长连接把消息推送到系统(HR 在群里 @机器人 即可,无需转发)
3. 大模型解析出:`姓名=张三 | 岗位=后端工程师 | 阶段=一面 | 状态=通过 | 期望薪资=15000 | 备注=二面约周四`
4. 数据写入数据库,自动同步腾讯在线文档
5. 招聘看板实时刷新,管理层随时可看漏斗与转化率

---

## 三、AI 工具 / 模型的实际应用能力(重点)

本方案不是"套壳",而是把大模型用在真实业务链路上,核心能力如下:

### 3.1 自然语言 → 结构化数据(核心亮点)

把 HR 口语化、零散的表达转成标准字段,替代人工填表。

| HR 原始说法 | 大模型解析结果 |
| --- | --- |
| 今天张三过了一面,后端岗,期望 15k,二面约周四 | `{"name":"张三","position":"后端工程师","stage":"一面","status":"通过","salary":15000,"note":"二面约周四"}` |
| 李四 offer 了,前端,12k,周五入职 | `{"name":"李四","position":"前端工程师","stage":"Offer","status":"通过","salary":12000,"note":"周五入职"}` |
| 王五那个产品经理简历不合适,pass 掉 | `{"name":"王五","position":"产品经理","stage":"淘汰","status":"不通过","note":"简历不合适"}` |

**即使一句话包含多人、多阶段,大模型也能一次性拆分解析。** 这就是传统正则/表单无法替代的价值。

### 3.2 智能清洗与纠错

- 自动统一阶段、状态枚举(「挂了/没戏/PASS」→「不通过」;「Offer/口头 offer」→「Offer」)
- 薪资单位归一化(「15k」「1.5万」→ `15000`)
- 同音字、别名纠正(「后端/后台/JAVA」→ 统一岗位规范)

### 3.3 智能提醒与跟进(可扩展)

基于"阶段 + 停留天数"的规则,由 AI 生成提醒文案,如:「张三 已进入二面等待 3 天,建议跟进」,推送到企业微信群。

### 3.4 自然语言报表问答(可扩展)

HR 直接问:「上周面试通过率多少?」「Offer 但未入职的有哪些?」,大模型转成查询并返回图表,实现"数据随口问"。

---

## 四、技术选型与成本估算

### 4.1 技术栈

| 层 | 技术 | 说明 |
| --- | --- | --- |
| 接入 | 企业微信智能机器人(长连接) | 免费、无需公网 URL/备案域名,群里 @机器人 即接收 |
| AI 解析 | DeepSeek API(deepseek-chat) | 国内可用、成本极低、中文理解强 |
| 兜底 | Python 规则引擎 | 无网络/无 Key 时也可运行(本 Demo 已内置) |
| 服务 | Python 标准库 HTTP Server | Demo 零依赖开箱即跑 |
| 存储 | SQLite | 免部署、单文件、够用 |
| 同步 | 腾讯文档(CSV 导入 / 企微文档 API) | 免费优先 |
| 可视化 | 单文件 HTML + Tailwind + ECharts | 无需构建,浏览器即开 |

### 4.2 成本估算(极简预算)

| 项目 | 费用 | 说明 |
| --- | --- | --- |
| DeepSeek API | **约 1 元/月** | 单条消息约 400 token,按每天 50 条 ≈ 6 万 token/月,费用可忽略 |
| 服务器 | 0 元(内网电脑)/ 约 30 元/月(轻量云) | 起步阶段用内网即可 |
| 腾讯在线文档 | 0 元 | 免费版 + CSV 同步;进阶走企微文档 API |
| 企业微信 | 0 元 | 已有载体,标准账号即可 |
| **合计** | **≈ 0 ~ 50 元/月** | |

> 相比 HR 每周手动录入数小时的人力成本,该方案投入几乎可忽略。

---

## 五、可运行 Demo(本目录)

### 5.0 在线预览与代码链接

| 交付物 | 地址 |
| --- | --- |
| 在线预览(浏览器直接打开,无需部署) | https://chengzhuoch.github.io/recruit-ai/demo/dashboard.html |
| 代码仓库 | https://github.com/CHENGZHUOCH/recruit-ai |
| 可运行 Demo 附件(含演示数据) | 见提交邮件附件 `AI招聘数据管家.zip` |

> 在线预览为内置演示数据;本地运行 `demo/app.py` 后展示实时数据。

### 5.1 Demo 说明

Demo 位于 [`./demo`](./demo),**核心零第三方依赖,Python 3 直接运行**,完整演示"消息 → 解析 → 入库 → 看板"全链路,且已打通企业微信智能机器人真机接收。

```bash
cd demo
python seed_demo.py      # 1. 生成一批演示数据
python app.py            # 2. 启动服务,浏览器打开 http://127.0.0.1:8080
```

- **看板**:启动后访问 `http://127.0.0.1:8080` 即可看到招聘漏斗/趋势/明细
- **真机接入(已打通)**:双击「启动智能机器人.bat」,HR 在企业微信群里 @机器人 发一句话 → 自动解析入库并回复确认
- **腾讯文档同步**:`python sync_tencent_docs.py` 导出 CSV,直接导入腾讯在线文档
- **接入真实大模型**:在 `demo/config.py` 填入 `DEEPSEEK_API_KEY`,即启用 DeepSeek 语义解析(不填则走内置规则引擎,同样可跑)

> 看板为纯静态单文件,可部署到任意静态托管(如 GitHub Pages、腾讯云 COS)实现永久在线预览。

---

## 六、实施路径

| 阶段 | 内容 | 周期 |
| --- | --- | --- |
| **第 1 阶段 · 试点** | 内网部署 Demo,HR 手动/半自动汇报,校验解析准确率,建同步模板 | 1 周 |
| **第 2 阶段 · 接入企微** | 接入企业微信智能机器人(长连接),群内 @机器人 直接汇报,开启自动同步腾讯文档 | 1~2 周 |
| **第 3 阶段 · 推广** | 全员招聘群接入,补充智能提醒、报表问答,沉淀岗位/阶段词典 | 2~4 周 |
| **第 4 阶段 · 沉淀** | 复盘准确率、迭代提示词,逐步替代手工统计 | 持续 |

**落地关键点**:① 预置岗位/阶段字典减少歧义;② 解析结果先人工抽查再放权自动入库;③ 同步模板与腾讯文档字段一一对应。

---

## 七、风险与应对

| 风险 | 应对 |
| --- | --- |
| 大模型偶尔解析不准 | 规则引擎兜底 + 解析结果页面人工可改 + 高置信才自动入库 |
| 消息格式自由度过高 | 提供常用话术模板(可直接复制到群里) |
| 数据安全 | 支持内网部署,敏感数据不出内网;可改用私有化模型(Ollama + Qwen) |
| 成本失控 | DeepSeek 单 token 极便宜,设置每日用量上限告警 |

---

## 八、扩展方向

- **多端覆盖**:消息入口扩展至邮件、飞书、HR 系统
- **自动简历解析**:候选人投递 PDF 简历,由多模态模型自动抽取字段入库
- **智能排期**:AI 基于面试官日历自动安排二面/三面
- **人才库沉淀**:淘汰候选人自动进人才库,支持"随口问"匹配复用

---

*方案配套可运行 Demo:见 `./demo/README.md`*
